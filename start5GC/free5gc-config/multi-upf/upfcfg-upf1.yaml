version: 1.0.3
description: Generic UPF 1 configuration

nrfUri: http://nrf.free5gc.org:8000

pfcp:
  addr: upf1.free5gc.org
  nodeID: upf1.free5gc.org
  retransTimeout: 1s
  maxRetrans: 3

sbi:
  scheme: http
  registerIPv4: upf1.free5gc.org
  bindingIPv4: upf1.free5gc.org
  port: 8000

gtpu:
  forwarder: userspace
  ifList:
    - addr: upf1.free5gc.org
      type: N3
  userspace:
    workers: 4
    queueSize: 1024
    tunName: upfusr1
    tunMtu: 1400

dnnList:
  - dnn: internet
    cidr: 10.60.0.0/16
    natifname: eth0

logger:
  enable: true
  level: debug
  reportCaller: false
